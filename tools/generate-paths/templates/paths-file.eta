<% /* @formatter:off */ %>
import { del, get, head, patch, post, put, typedFetch } from "./typed-fetch";
import type {
<% it.typeImports.forEach((type) => { %>
	<%= type %>,
<% }) %>
} from './types-<%= it.key %>';
const _enc = (p: string) => encodeURIComponent(p);

<% it.paths.forEach((path) => { %>
	<%_ = path %>

<% }) %>